# mini_agent — Architecture & State

A coding agent powered by DeepSeek V4 Pro. Terminal REPL + Textual TUI.
562 tests, 31 tools, SQLite memory.

## This Session (2025-05-17)

### Event-driven TUI (no polling)
- Removed `set_interval(0.08, self._drain)` — was 12.5 Hz polling
- `_NotifyQueue.put()` now calls `app.call_from_thread(app._drain)` on every enqueue
- Drain only fires when messages arrive; zero CPU when idle
- Token-by-token streaming: `_flush_logs()` after each content chunk in `_handle_token`
- User input fix: `_flush_logs()` after user message box in `_submit` (was waiting for drain)

### Multi-agent depth > 1
- Removed hard block on spawn/status/collect in sub-agents
- Depth tracking: `_agent_depth` + `_agent_max_depth` in `_TOOL_CONTEXT`
- Max depth default: 3. Tools blocked only at max depth, not depth 1.
- `_spawn_one`: passes `parent_depth` to `run_sub_agent`, sets context for nested spawns
- Sub-agents get full orchestrator instructions when depth < max
- Depth info injected as system message (e.g. "at depth 2/3 — you MAY spawn sub-agents")
- `_spawn_agent`: reads parent depth from context, passes through

### Sub-agent tree display
- `#agent-tree` in static-pane (right side of tools-log) — hidden when no sub-agents
- Shows on first spawn (`display: block`), hides when all done
- Nested hierarchy: `_agent_task_id` as parent_id → children appear indented under parent
- Nodes: [RUN] → [OK]/[ERR] on completion
- No emojis: `⚙` replaced with `[tool]` text labels

### Memory fixes
- `memory.py`: `set_scratchpad`, `save_test_output`, `clear` — added `conn.commit()`
- Shared connection (`_get_conn`) + incremental token accumulator

### Bug Fixes (167 total)

## Test Suite: 562 tests across 18 files, 0 failed

## Previous Session (2025-05-16)

### Bug fixes (2)
- **eval/scorer.py**: `_check_shell` called `subprocess.run()` without `args` positional —
  `cmd` was parsed via `shlex.split()` but never passed to the call. All shell checks
  crashed with `Popen.__init__() missing 1 required positional argument: 'args'`.
- **tools/__init__.py**: `_fix_unquoted_keys` split non-quoted text character-by-character
  (one list element per char), so the regex `(\w+)(\s*:)` could never match across multiple
  characters. Changed to accumulate consecutive non-quoted chars into segments, matching the
  same segment-level logic used for quoted strings. Fixes `test_unquoted_keys` and
  `test_combined_malformations` in test_resilience.py.

### Test suite: 562 passed, 0 failed

### Scratchpad SQLite table bug fix
- `_write_scratchpad` (file_ops.py), `set_scratchpad` / `get_scratchpad` (memory.py),
  and `_persist_test_output` (shell_ops.py) all opened raw SQLite connections and
  executed INSERT/REPLACE without ensuring the target table existed.
- The scratchpad table is created in `MemoryStore.__init__`, but in test scenarios
  `_TOOL_CONTEXT.scratchpad_path` could point at a fresh DB never initialized that way.
- Fix: Added `CREATE TABLE IF NOT EXISTS` + `INSERT OR IGNORE` (seed row) before every
  DML statement in all four locations. Also changed `set_scratchpad` from silently
  swallowing errors to printing a warning to stderr.

### Sub-agent completion signals (orchestration awareness)
- AgentRuntime: added `_seen_completions` set to track which results have been surfaced
- Added `get_pending_results()` — returns newly-completed results since last call
- Added `get_running_ids()` — returns task_ids of all currently running sub-agents
- `run_agent_turn()` now injects orchestration context at turn start:
  - Shows newly-completed sub-agent results with status
  - Lists still-running sub-agents with reminder to poll
  - Transient message (not persisted to memory)
- Fixed pre-existing deadlock: `store_result` called `clear_inbox` while holding
  `self._lock`; `clear_inbox` also acquires `self._lock` (plain Lock, not re-entrant).
  Inlined the inbox cleanup directly in `store_result`.

### Orchestrator mode prompt (prompt.py)
- Added explicit "Orchestrator mode" section to system prompt
- After spawning sub-agents, parent must act as orchestrator only
- Rules: poll with agent_status, extend proactively, never duplicate delegated work
- collect_agent timeout is NOT failure — extend and retry
- Cancel only when: 35 turns exhausted OR 3+ consecutive error loops
- Scratchpad must track task IDs, extensions, and collected results

## Previous Session (2025-05-13)

### Massive Bug-Squash (from improvements.txt audit)
- Spawned 5 sub-agents to review entire codebase → wrote ~150 findings to improvements.txt
- Spawned 4 fix agents to address all findings in parallel across ~20 files

### Subsystem Fixes
| Subsystem | HIGH | MED | LOW | Key changes |
|-----------|------|-----|-----|-------------|
| Core runtime | 4 | 6 | 5 | lock for _MODIFIED_FILES, UnboundLocalError on msg, ellipsis truncation, _min_turn counter, _git_diff_injected, force-flush stream end, configurable retry timeouts |
| Agent runtime + comms | 4 | 10 | 6 | DEADLOCK FIX (notify outside _lock), lost-wakeup fix (wait_for), garbled handoff fix, inbox memory leak cleanup, fan_out/scatter_gather subscriptions, barrier condition.wait |
| Tools | 7 | 8 | 5 | JSON repair string-safe, cache key mismatch, backup timestamp collisions, reindex on edit, scratchpad safety, task registry cleanup, stream cap, LRU ordering, MCP stderr drain, TOOLS dedup |
| Memory + Config | 3 | 7 | 9 | redundant _ensure_table removed, _last_saved_count in try, explicit rollback, lazy requests import, atexit session close, unknown arg warn, os.walk guard, empty workspace guard |
| Eval | 3 | 5 | — | turns_used from _turn_count, max_turns or→if None, on_tool_end tracking, binary file crash, shlex crash, --allow-empty git commit, bash_hello→file check |

### collect_any blocking fix
- _COLLECT_ANY_TIMEOUT: 120s → 10s
- _COLLECT_TIMEOUT: 120s → 30s
- Timeout message now lists still-running task IDs for retry

### TUI: Queued messages now purple
- TuiTheme gained `purple` field across all 8 themes
- Queued interjections display with purple box chrome instead of dim

### Bug Fixes (17 + ~150 = ~167 total)

## Test Suite: 466 tests across 18 files
- test_tools.py (99), test_mcp.py (47), test_resilience.py (40), test_tui.py (37)
- test_eval.py (38), test_safety.py (33), test_memory.py (25), test_agent_messages.py (23)
- test_config.py (18), test_agent_loop.py (17), test_git.py (16), test_sub_agent.py (53)
- test_terminal.py (11), test_parse_stream.py (8), test_lint.py (0)

## Directory Map
```
[workspace root]/
  mini_agent.py        Terminal REPL entry point
  tui.py               Textual TUI
  llm.py               LLM comms, agent loop
  prompt.py            System prompt
  config.py            AgentConfig: TOML + env + CLI
  memory.py            SQLite conversation store
  safety.py            Read/Write safety gates
  terminal.py          ANSI colour helpers
  retry.py             HTTP retry with jitter
  stream.py            SSE stream parser
  agent_runtime.py     SubAgentResult + AgentRuntime
  sub_agent.py         run_sub_agent()
  interject.py         Thread-safe user interjection queue
  improvements.txt     Audit findings (~150 items, all fixed)
  tools/
    __init__.py        ToolResult, dispatch, cache, JSON repair
    schema.py          TOOLS list
    file_ops.py        read/write/edit/list/info/scratchpad/diff/restore/plan
    shell_ops.py       run_shell, search_files, run_tests, git, task_status, verify
    search_ops.py      find_symbol, find_usages, semantic_search, web_search, recall_turn
    agent_ops.py       spawn/status/collect/collect_any/message/read/extend/handoff/inbox/subscribe
    agent_messages.py  AgentMessage, MSG_TYPE_REGISTRY, validation, routing
    agent_patterns.py  fan_out, fan_in, pipeline, barrier, scatter_gather
    mcp_client.py      McpConnection, McpClientManager, MCP JSON-RPC over stdio
  eval/
    __init__.py, runner.py, scorer.py, metrics.py
    tasks/ (5 YAML tasks), fixtures/mini_repo/, reports/
  tests/
    mcp_echo_server.py MCP test fixture
