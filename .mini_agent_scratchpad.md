# Session Summary — 2026-05-11

## Completed (10 commits)
1. Lower DEFAULT_MAX_MESSAGES 500→300
2. Dedup tool list + safe TUI table rendering
3. Single-pass symbol index, sem search mtime guard, DRY export, DRY workspace
4. Fix TUI crash v1: flush _table_buf at turn end
5. Startup context injection (file tree + STATE + git log)
6. Pre-turn checkpoint, edit_file hint, standalone diff tool
7. Scratchpad prominence + smart test detection
8. Updated --help with all flags, env vars, config file
9. Scratchpad staleness nudge: prompt rule + mechanical warning
10. TUI [/code] crash fix v2: single write() for table flush

## In progress
- Fix scratchpad not persisting across write_scratchpad calls
  - Root cause: tui.py's set_context() was missing scratchpad_path
  - Fix: added scratchpad_path=self.memory._db_path to tui.py

## Self-assessment lessons (for next session)
- Update scratchpad every 3-4 turns — the mechanical nudge works!
- When edit_file fails, run_shell+sed to verify exact content before retry
- Batch parallel reads when independent
- The STATE.txt "trailing ```" was a diff display artifact, not a real file issue
